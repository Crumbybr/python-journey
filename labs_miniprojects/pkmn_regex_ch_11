import csv
import re

name = {}
pkmn= "/Users/brandon/Documents/1_Documents/1_action_folders_perm/1_docs_perm/1_courses/3_fcc/py4e/labs_miniprojects/pokemon.csv"

with open(pkmn) as f:
    for line in f:
        
        if line.startswith('106'):
            #line.split(',') #attempt to split the line but it's already a .csv
            #specs = re.findall('\s',line) 
            #pkmn_type = specs[0]
            #line[0:5].find()
            print(line)
            
            #name.items(line) #attempted to make it a tuple
            #name[2,line] 
#print(name)